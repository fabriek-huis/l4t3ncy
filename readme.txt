*warning* to improve accuracy increase the runCount's and payload size, even though i built this as a small lightweight dection script to check for bandwidth speeds when deploying different video bitrates. 